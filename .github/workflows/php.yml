name: Pull Main Branch

on:
  push:
    branches:
      - main

jobs:
  pull_main_branch:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout main branch
      uses: actions/checkout@v3
      with:
        ref: main
